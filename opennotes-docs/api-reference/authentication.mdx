---
title: Authentication
description: Bearer-token auth on /api/public/v1 using a platform:adapter-scoped API key.
---

<Note>
  Placeholder — this page is filled in by **TASK-1460.05**. The final content
  documents the `Authorization` header, the `platform:adapter` scope
  requirement, and how auth failures surface in responses.
</Note>
