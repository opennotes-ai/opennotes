---
title: Authentication
description: Every request to /api/public/v1 carries a platform:adapter-scoped API key via the Authorization header.
---

<Note>
  Placeholder — this page is filled in by **TASK-1460.02**. The final content
  documents the `Authorization: Bearer <api_key>` header, failure modes for
  invalid/expired/wrong-scope keys, and how the server distinguishes adapters
  from platform callers.
</Note>
