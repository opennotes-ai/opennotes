---
title: "Step 1 — Identify the user"
description: Use GET /api/public/v1/user-profiles/lookup to map a platform user to an OpenNotes profile.
---

<Note>
  Placeholder — this page is filled in by **TASK-1460.03**. The final content
  walks through `user-profiles/lookup` with real JSON and points at the
  Discourse `OpenNotes::Client#lookup_user_profile` call site.
</Note>
